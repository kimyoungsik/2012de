<% if @user == current_user %>
<div class="main-box photo-new">
	<div class="item-title">
		<h2>프로필 사진 수정하기</h2>
		<span>본인의 프로필 사진을 수정합니다.</span>
	</div>
	
	<div class="item-box">
		<p>내 컴퓨터에 있는 이미지 파일을 선택해주세요.</p>
		<span>* jpg, png, gif, bmp파일 모두 선택하실 수 있습니다.</span>
		
		<div id="photo-selector">사진 고르기
			<%= form_for @photo, :html => { :multipart => true } do |f| %>
				<%= f.file_field :image, :class => "" ,:accept => 'image' %>
				<%= f.hidden_field :photoable_id %>
				<%= f.hidden_field :photoable_type %>
				<div><%= f.submit "편집하기", :id=> "photo-submit" %></div>
			<% end %>
		</div><span id="photo-text"></span>
	</div>
</div>
<% end %>
